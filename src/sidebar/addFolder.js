import React from 'react';
import { Link } from 'react-router-dom';

export default function AddFolder() {
    return (
        <li className="add-folder-container">
            <Link to={'/'}>AddFolder</Link>
        </li>
    )
}